# Alcovia Frontend (Expo Web)

Run in development:

```powershell
cd frontend
npm install
npx expo start --web
```

This is an Expo-based React Native app that runs on the web. It connects to the backend at `REACT_APP_BACKEND_URL` or `http://localhost:4000` by default.
